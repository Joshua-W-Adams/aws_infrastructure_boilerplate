# IP Blocking Architectures

A few architectures to block IPs are explained as follows:

Blocking at the ALB.

![](./../../../img/alb_waf_ip_blocking.png)

Blocking at CloudFront Distributions:

![](./../../../img/cloudfront_waf_ip_blocking.png)